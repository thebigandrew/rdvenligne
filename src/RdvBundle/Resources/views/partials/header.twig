<!-- Logo -->
<a href="{{ path('dashboard') }}" class="logo">
    <!-- mini logo for sidebar mini 50x50 pixels -->
    <span class="logo-mini">
		<img src="{{ asset('images/RDV-Online.png') }}" height="35" width="35" alt="RdvEnLigne"/>
	</span>
    <!-- logo for regular state and mobile devices -->
    <span class="logo-lg">
		<img src="{{ asset('images/RDV-Online.png') }}" height="35" width="59" alt="RdvEnLigne"/>
	</span>
</a>

<!-- Header Navbar -->
<nav class="navbar navbar-static-top" role="navigation">
    <!-- Sidebar toggle button-->
    <a href="#" class="sidebar-toggle" data-toggle="offcanvas" role="button">
        <span class="sr-only">Toggle navigation</span>
    </a>
    <!-- Navbar Right Menu -->
    <div class="navbar-custom-menu">
        <ul class="nav navbar-nav">
            <!-- User Account Menu -->
            <li class="dropdown user user-menu">
                <a href="{{ path('user-profile') }}">
                    <!-- The user image in the navbar-->
                    <div class="avatar-circle">
                        {# <span class="initials">{{ user.firstname|first|capitalize }}{{ user.lastname|first|capitalize }}</span> #}
                        <span class="initials">Vincent ABET</span>
                    </div>
                    <!-- hidden-xs hides the username on small devices so only the image appears. -->
                    {# <span class="hidden-xs username">{{ user.firstname }} {{ user.lastname }}</span> #}
                </a>
            </li>
            <!-- Control Sidebar Toggle Button -->
            <li>
                <a href="{{ path('logout') }}"><i class="fa fa-sign-out"></i></a>
            </li>
        </ul>
    </div>
</nav>

